[build]
  command = "corepack enable pnpm && pnpm install && pnpm --filter @workspace/simran-os run build"
  publish = "artifacts/simran-os/dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
